<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign-in</title>

  <link rel="icon" href="/images/logo.jpg">

  <!--API Links-->
  <!--Bootstrap Links-->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

  <!-- CSS Linsks -->
  <link rel="stylesheet" href="/stylesheets/header.css">
  <link rel="stylesheet" href="/stylesheets/main.css">
  <link rel="stylesheet" href="/stylesheets/sign-in-sign-up.css">

</head>
  <body>
    
    <!-- START OF HEADER -->
    <header>
      <div class="logo">
        <img src="/images/amazon-logo.png" alt="logo">
      </div>

      <div class="company-name">
        <h4>Amazon Philippines</h4>
      </div>

      <div class="dropdown">
        <ul class="nav nav-tabs" style="border: none;">
          <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false" style="color: white; border: none;">
                <i class="bi bi-person-circle" style="color: white;
                  font-size: 25px;"></i>
              </a>
            <ul class="dropdown-menu" style="border: none;">
              <li><a class="dropdown-item" href="/product-connection">Home</a></li>
              <li><a class="dropdown-item" href="/sign-in-sign-up">Sign-up</a></li>
              <li><a class="dropdown-item" href="/sign-in-sign-up">Sign-in</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">About Us</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </header>
    <!-- END OF HEADER -->

    <!-- START OF FORM -->
    <main>
      <div class="container" style="height: 500px;">
        <div class="forms-container">
          <div class="form-control signup-form" 
            id="sign-up-form" style=" height: 500px;">
            <h2>Sign-up</h2>

            <form action="/sign-up" method="POST" id="reg-form">
              <div class="left-reg-form">
                <input type="text" name="firstName" placeholder="First Name" value="<%= formData?.firstName || '' %>" required>
                <input type="text" name="userName" placeholder="Username" value="<%= formData?.userName || '' %>" required />
                <input type="email" name="email" placeholder="Email" value="<%= formData?.email || '' %>" required />
                <input type="password" name="password" placeholder="Password" required />
              </div>
              <div>
                <input type="text" name="lastName" placeholder="Last Name" value="<%= formData?.lastName || '' %>" required>
                <input type="number" name="contactNumber" placeholder="Contact Number" value="<%= formData?.contactNumber || '' %>" required />
                <input type="number" name="age" placeholder="Age" value="<%= formData?.age || '' %>" required />
                <input type="password" name="confirmPassword" placeholder="Confirm Password" required />
              </div>
              <button type="submit" id="submit-reg">Sign-up</button>
            </form>

            <!-- Display error messages -->
            <% if (errors && errors.length > 0) { %>
              <div class="error-message" id="error-messages">
                <% errors.forEach(error => { %>
                  <%= error.msg %>
                <% }) %>
              </div>
            <% } %>

            <span>or sign-up with</span>
            <div class="socials">
              <i class="fab fa-facebook-f"></i>
              <i class="fab fa-google-plus-g"></i>
              <i class="fab fa-linkedin-in"></i>
            </div>
          </div>
          <div class="form-control signin-form" style="height: 500px;">

            <form action="/sign-in" method="POST">
              <h2>Sign-in</h2>
              <input type="text" name="userName" placeholder="Username" required />
              <input type="password" name="password" placeholder="Password" required />
              <button type="submit">Sign-in</button>
               <!-- Display error message if present -->
              <% if (typeof error !== 'undefined') { %>
                <div class="error-message">
                  <%= error %>
                </div>
              <% } %>
            </form>

            <span>or sign-in with</span>
            <div class="socials">
              <i class="fab fa-facebook-f"></i>
              <i class="fab fa-google-plus-g"></i>
              <i class="fab fa-linkedin-in"></i>
            </div>
            <span><a>Forgot password?</a></span>
          </div>
        </div>
        <div class="intros-container">
          <div class="intro-control signin-intro" style="height: 500px;">
            <div class="intro-control__inner">
              <h2>Welcome back!</h2>
              <p>
                Welcome back! We are so happy to have you here. It's great to see you again. We hope you had a safe and enjoyable time away.
              </p>
              <button id="signup-btn">No account yet? Sign-up.</button>
            </div>
          </div>
          <div class="intro-control signup-intro" style="height: 500px;">
            <div class="intro-control__inner">
              <h2>Come join us!</h2>
              <p>
                We are so excited to have you here.If you haven't already, create an account to get access to exclusive offers, rewards, and discounts.
              </p>
              <button id="signin-btn">Already have an account? Sign-in.</button>
            </div>
          </div>
        </div>
      </div>
    </main>
    <!-- END OF FORM -->
    
    <script src="/javascripts/sign-in-sign-up-slider.js"></script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  </body>
</html>